global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts/*.yml

scrape_configs:
  - job_name: helios-control-plane
    static_configs:
      - targets: ["control-plane:8080"]
  - job_name: helios-planner
    static_configs:
      - targets: ["planner:8090"]
