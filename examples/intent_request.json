{
  "name": "order-risk-ai-planned-pipeline",
  "intent": "validate order records, normalize fields, run model inference, aggregate risk metrics, write an artifact, and notify operations",
  "timeout_seconds": 20,
  "retry_policy": {
    "max_attempts": 3,
    "initial_backoff_seconds": 2,
    "max_backoff_seconds": 20,
    "multiplier": 2
  }
}
